# OpenAlias — Environment Configuration
# Copy this file to .env and fill in your values.

# ── Application ──────────────────────────────────────
APP_URL=http://localhost
DB_PATH=data/openalias.sqlite

# ── SSO / OIDC (optional) ───────────────────────────
# Leave blank to disable SSO and use local auth only.
#
# Keycloak example:
#   SSO_PROVIDER_URL=https://auth.example.com
#   SSO_REALM=master
#
# Generic OIDC (Auth0, Google, etc.):
#   SSO_PROVIDER_URL=https://accounts.google.com
#   SSO_REALM=            (leave empty)
#
SSO_PROVIDER_URL=
SSO_REALM=
SSO_CLIENT_ID=
SSO_CLIENT_SECRET=
